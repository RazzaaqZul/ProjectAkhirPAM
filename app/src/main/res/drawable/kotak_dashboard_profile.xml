<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/gradient" /> <!-- Warna latar belakang bentuk -->
    <corners android:radius="10dp" /> <!-- Radius sudut (opsional) -->
    <stroke
        android:width="2dp"
        android:color="@color/gradient" />
</shape>